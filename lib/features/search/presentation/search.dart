import 'package:flutter/material.dart';

class FindStudioPage extends StatelessWidget {
  const FindStudioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Find Studio'),
      ),
    );
  }
}
