import 'package:flutter/material.dart';

class FavoriteStudioPage extends StatelessWidget {
  const FavoriteStudioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favorite Studio'),
      ),
    );
  }
}
